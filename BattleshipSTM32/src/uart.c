#include "stm32f0xx.h"
#include "uart.h"

void UART_Init(void) {
    // UART Initialization code
}

void UART_SendString(const char* str) {
    // Code to send a string over UART
}

char UART_ReceiveChar(void) {
    // Code to receive a character over UART
}
